# CivicSeva Backend

Minimal Express + Mongoose scaffold for the CivicSeva project.

Quick start

1. Copy `.env.example` to `.env` and update values.
2. Install dependencies:

```bash
cd Backend
npm install
```

3. Run in development:

```bash
npm run dev
```
